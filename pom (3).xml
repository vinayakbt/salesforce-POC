<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>bd865388-5998-4b4c-b08c-71ffec24085e</groupId>
	<artifactId>testing-ch2-0-dynamic-version-update</artifactId>
<!-- 	<version>1.0.0</version> -->
<!-- 	<version>${project.version}</version> -->
	<version>${revision}</version>
	<packaging>mule-application</packaging>
	<name>testing-ch2-0-dynamic-version</name>
	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<app.runtime>4.9.0</app.runtime>
		<revision>1.0.0</revision>
		<mule.maven.plugin.version>4.3.1</mule.maven.plugin.version>
		<!-- <jfrog.maven.plugin.version>3.2.0</jfrog.maven.plugin.version> -->
		<jmeter.maven.plugin.version>2.8.5</jmeter.maven.plugin.version>
		<munit.version>3.4.0</munit.version>
		<app.applicationName>testing-ch2-0-dynamic-version</app.applicationName>
		<app.businessGroupId>bd865388-5998-4b4c-b08c-71ffec24085e</app.businessGroupId>
		<app.businessGroup>3M Master</app.businessGroup>
		<app.environment>Dev</app.environment>
		<app.deployPrefix>${app.prefixEnv}</app.deployPrefix>
		<app.buildNumber>1</app.buildNumber>
		<mule.log.level>INFO</mule.log.level>
		<maven.deploy.skip>false</maven.deploy.skip>
		<maven.download.plugin.version>1.6.8</maven.download.plugin.version>
		<serverId>Repository</serverId>
		<exchange.mule.maven.plugin.version>0.0.23</exchange.mule.maven.plugin.version>
		<app.replicas>1</app.replicas>
		<app.vCores>0.1</app.vCores>

	</properties>
	<distributionManagement>
		<repository>
			<id>${serverId}</id>
			<name>Corporate Repository</name>
			<url>https://maven.anypoint.mulesoft.com/api/v3/organizations/${app.businessGroupId}/maven</url>
			<layout>default</layout>
		</repository>
	</distributionManagement>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.10.1</version>
				<configuration>
					<compilerArgs>
						<args>-parameters</args>
					</compilerArgs>
					<target>17</target>
					<source>17</source>
				</configuration>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<configuration>
					<skip>${maven.deploy.skip}</skip>
				</configuration>
			</plugin>
			<!-- <plugin> <groupId>org.jfrog.buildinfo</groupId> <artifactId>artifactory-maven-plugin</artifactId> 
				<version>${jfrog.maven.plugin.version}</version> <inherited>false</inherited> 
				<executions> <execution> <id>build-info</id> <phase>install</phase> <goals> 
				<goal>publish</goal> </goals> <configuration> <deployProperties> <gradle>awesome</gradle> 
				</deployProperties> <artifactory> <includeEnvVars>true</includeEnvVars> <envVarsExcludePatterns>*password*,*secret*,*key*,*token*,*passphrase*</envVarsExcludePatterns> 
				<timeoutSec>300</timeoutSec> </artifactory> <publisher> <contextUrl>https://acelity.jfrog.io/acelity</contextUrl> 
				<username>${artifactory.username}</username> <password>${artifactory.password}</password> 
				<excludePatterns>*-tests.jar</excludePatterns> <repoKey>mule-libs-release</repoKey> 
				<snapshotRepoKey>mule-libs-snapshots</snapshotRepoKey> <publishArtifacts>${jfrog.publishArtifacts}</publishArtifacts> 
				<publishBuildInfo>${jfrog.publishBuildInfo}</publishBuildInfo> </publisher> 
				<buildInfo> <buildNumber>${app.environment}-${app.buildNumber}</buildNumber> 
				<buildName>${app.deployPrefix}${app.applicationName}</buildName> </buildInfo> 
				</configuration> </execution> </executions> </plugin> -->
			<plugin>
				<groupId>org.mule.tools.maven</groupId>
				<artifactId>mule-maven-plugin</artifactId>
				<version>${mule.maven.plugin.version}</version>
				<extensions>true</extensions>
				<configuration>
					<classifier>mule-application</classifier>
					<cloudhub2Deployment>
						<uri>https://anypoint.mulesoft.com</uri>
						<provider>MC</provider>
						<muleVersion>${app.runtime}</muleVersion>
						<javaVersion>17</javaVersion>
						<releaseChannel>LTS</releaseChannel>
						<username>${app.username}</username>
						<password>${app.password}</password>
						<applicationName>${app.deployPrefix}${app.applicationName}</applicationName>
						<environment>${app.environment}</environment>
						<target>${app.target}</target>
						<replicas>${app.replicas}</replicas>
						<vCores>${app.vCores}</vCores>
						<deploymentSettings>
							<generateDefaultPublicUrl>true</generateDefaultPublicUrl>
						</deploymentSettings>
						<properties>
							<mule_target_env>${mule_target_env}</mule_target_env>
							<splunkIndex>${app.splunkIndex}</splunkIndex>
							<splunkEnv>${app.splunkEnv}</splunkEnv>
							<appName>${app.deployPrefix}${app.applicationName}</appName>
						</properties>
						<server>${serverId}</server>
						<secureProperties>
							<anypoint.platform.client_id>${app.client_id}</anypoint.platform.client_id>
							<anypoint.platform.client_secret>${app.client_secret}</anypoint.platform.client_secret>
							<secret.key>${secret.key}</secret.key>
							<splunkToken>${app.splunkToken}</splunkToken>
						</secureProperties>
					</cloudhub2Deployment>
					<executions>
						<execution>
							<id>deploy</id>
							<phase>deploy</phase>
							<goals>
								<goal>deploy</goal>
							</goals>
						</execution>
					</executions>
					<sharedLibraries>
						<sharedLibrary>
							<groupId>org.apache.activemq</groupId>
							<artifactId>activemq-client</artifactId>
						</sharedLibrary>
						<sharedLibrary>
							<groupId>com.oracle.database.jdbc</groupId>
							<artifactId>ojdbc8</artifactId>
						</sharedLibrary>
					</sharedLibraries>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.mulesoft.munit.tools</groupId>
				<artifactId>munit-maven-plugin</artifactId>
				<version>${munit.version}</version>
				<executions>
					<execution>
						<id>test</id>
						<phase>test</phase>
						<goals>
							<goal>test</goal>
							<goal>coverage-report</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<coverage>
						<runCoverage>true</runCoverage>
						<formats>
							<format>html</format>
							<format>console</format>
						</formats>
						<ignoreFiles>
							<ignoreFile>common-error-handler.xml</ignoreFile>
						</ignoreFiles>
					</coverage>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.mule.tools.maven</groupId>
				<artifactId>exchange-mule-maven-plugin</artifactId>
				<version>${exchange.mule.maven.plugin.version}</version>
				<dependencies>
					<dependency>
						<groupId>com.googlecode.maven-download-plugin</groupId>
						<artifactId>download-maven-plugin</artifactId>
						<version>${maven.download.plugin.version}</version>
					</dependency>
				</dependencies>
				<executions>
					<execution>
						<id>exchange-pre-deploy</id>
						<phase>pre-integration-test</phase>
						<goals>
							<goal>exchange-pre-deploy</goal>
						</goals>
					</execution>
				</executions>
			</plugin>

			<!-- Removed additional  Plugin exchange-deploy -->

		</plugins>
		<finalName>${project.artifactId}-${project.version}-BLD-${app.buildNumber}</finalName>
	</build>
	<dependencies>
		<dependency>
			<groupId>com.solventum.kci</groupId>
			<artifactId>kcilogger-plugin</artifactId>
			<version>1.0.6</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-core</artifactId>
			<version>2.17.2</version>
		</dependency>
		<dependency>
			<groupId>com.solventum</groupId>
			<artifactId>common-framework-handler</artifactId>
			<version>1.0.3</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>org.apache.logging.log4j</groupId>
			<artifactId>log4j-api</artifactId>
			<version>2.17.2</version>
		</dependency>
		<dependency>
			<groupId>com.splunk.logging</groupId>
			<artifactId>splunk-library-javalogging</artifactId>
			<version>1.11.7</version>
		</dependency>
		<dependency>
			<groupId>org.apache.activemq</groupId>
			<artifactId>activemq-client</artifactId>
			<version>5.15.4</version>
		</dependency>
		<dependency>
			<groupId>com.mulesoft.modules</groupId>
			<artifactId>mule-secure-configuration-property-module</artifactId>
			<version>1.2.7</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>com.mulesoft.munit</groupId>
			<artifactId>munit-runner</artifactId>
			<version>3.4.0</version>
			<classifier>mule-plugin</classifier>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.mulesoft.munit</groupId>
			<artifactId>munit-tools</artifactId>
			<version>3.4.0</version>
			<classifier>mule-plugin</classifier>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mule.modules</groupId>
			<artifactId>mule-apikit-module</artifactId>
			<version>1.11.5</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>org.mule.connectors</groupId>
			<artifactId>mule-jms-connector</artifactId>
			<version>1.10.1</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>com.mulesoft.connectors</groupId>
			<artifactId>mule-salesforce-connector</artifactId>
			<version>11.1.1</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>org.mule.connectors</groupId>
			<artifactId>mule-db-connector</artifactId>
			<version>1.14.15</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<!-- <dependency> -->
		<!-- <groupId>org.mule.module</groupId> -->
		<!-- <artifactId>mule-java-module</artifactId> -->
		<!-- <version>1.2.14</version> -->
		<!-- <classifier>mule-plugin</classifier> -->
		<!-- </dependency> -->
		<dependency>
			<groupId>org.mule.connectors</groupId>
			<artifactId>mule-sockets-connector</artifactId>
			<version>1.2.5</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>org.mule.connectors</groupId>
			<artifactId>mule-http-connector</artifactId>
			<version>1.10.3</version>
			<classifier>mule-plugin</classifier>
		</dependency>
		<dependency>
			<groupId>com.oracle.database.jdbc</groupId>
			<artifactId>ojdbc8</artifactId>
			<version>23.8.0.25.04</version>
		</dependency>
	</dependencies>

	<repositories>
		<repository>
			<id>mulesoft-releases</id>
			<name>MuleSoft Releases Repository</name>
			<url>https://nexus.udapp-appsec.us.amz.3mhis.net/repository/dsc-maven-us-east-1-group/</url>
			<layout>default</layout>
		</repository>
		<repository>
			<id>anypoint-exchange-v3</id>
			<name>Anypoint Exchange V3</name>
			<url>https://nexus.udapp-appsec.us.amz.3mhis.net/repository/dsc-maven-us-east-1-group/</url>
			<layout>default</layout>
		</repository>
		<repository>
			<id>splunk-artifactory</id>
			<name>Splunk Releases</name>
			<url>https://nexus.udapp-appsec.us.amz.3mhis.net/repository/dsc-maven-us-east-1-group/</url>
		</repository>
		
		<repository>
			<id>${serverId}</id>
			<name>Private Exchange repository</name>
			<url>https://maven.anypoint.mulesoft.com/api/v3/organizations/${app.businessGroupId}/maven</url>
			<layout>default</layout>
		</repository>
	</repositories>
	<pluginRepositories>
		<pluginRepository>
			<id>mulesoft-releases</id>
			<name>MuleSoft Releases Repository</name>
			<layout>default</layout>
			<url>https://nexus.udapp-appsec.us.amz.3mhis.net/repository/dsc-maven-us-east-1-group/</url>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</pluginRepository>
	</pluginRepositories>
</project>
